FROM node:18-alpine
WORKDIR /app

# Copy package files
COPY package*.json ./

# Install dependencies (including devDependencies for express, cors, etc.)
RUN npm install

# Copy proxy server file
COPY proxy-server.js ./

EXPOSE 3001

CMD ["node", "proxy-server.js"]
